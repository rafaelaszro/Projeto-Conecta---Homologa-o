/**
 * Cadastro de usuário — requisito "Usuário se cadastra na plataforma".
 * Implementação prevista para a sequência do Incremento 1.
 */

import { TelaEmConstrucao } from '@/components/TelaEmConstrucao';

export default function TelaCadastro() {
  return (
    <TelaEmConstrucao
      titulo="Criar conta"
      descricao="O formulário de cadastro e a solicitação de acesso a uma organização entram na sequência desta etapa."
      requisito="Incremento 1 · Cadastro de usuário"
    />
  );
}
